<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uts_h1101171042">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".provider"></activity>
        <activity android:name=".intent" />
        <activity android:name=".broadcast" />
        <activity android:name=".service" />
        <activity android:name=".Komponen_lima" />
        <activity android:name=".Komponen_empat" />
        <activity android:name=".Komponen_tiga" />
        <activity android:name=".Komponen_dua" />
        <activity android:name=".aktivitas" />
        <activity android:name=".Komponen_satu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>